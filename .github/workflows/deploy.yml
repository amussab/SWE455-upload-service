<<<<<<< HEAD
name: Deploy upload service
=======
name: Deploy Upload Service
>>>>>>> 64960c9 (Add CI/CD pipeline)

on:
  push:
    branches: [main]
<<<<<<< HEAD
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install test dependencies
        run: python -m pip install -r requirements.txt

      - name: Run tests
        run: python -m pytest tests/ -q

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION || 'us-east-1' }}

      - name: Build Lambda artifact
        run: zip -j upload.zip index.py

      - name: Upload artifact to S3
        run: aws s3 cp upload.zip s3://${{ vars.ARTIFACT_BUCKET }}/upload.zip

      - name: Update Lambda code
        run: |
          aws lambda update-function-code \
            --function-name upload-service \
            --s3-bucket ${{ vars.ARTIFACT_BUCKET }} \
            --s3-key upload.zip
=======

jobs:
  build-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Zip application
        run: zip -r upload.zip .

      - name: Upload to S3
        run: |
          aws s3 cp upload.zip s3://file-validation-artifacts-12345/upload.zip
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: us-east-1
>>>>>>> 64960c9 (Add CI/CD pipeline)
